# 20260724 总结
怎么是部分分训练啊啊啊（（\
省流：`namespace` 命名和库函数重名 T^T，太可怕了，~~有人给出库函数表吗~~，下次使用英文单词必须加大写，~~OI 赛制交题时必须用 lg 编辑器先测一下会不会 CE，保证不交~~。

## A
### 12pts 做法
暴搜 bfs。暴力算方差。\
$O(n!)$ 。\
这个复杂度等会儿再解释。

### 20pts（实际 24pts）做法
你说得对但是答案是 $n\sum_{i=1}^na_i^2-(\sum_{i=1}^na_i)^2$ 。\
依旧暴搜但是求 $\Delta ans$ 是 $O(1)$ 的。\
于是复杂度是 $O((n-1)!)$。

### 32pts 做法
不会。

### 100pts 做法
**注！意！到！操作是交换两个相邻的差分！**

~~可惜赛时我就没注意到~~\
~~注意到了也不会呀~~

然后搓！样！例！可得答案最小时差分呈现单谷。

：再然后呢？

然后 SA。并非后缀数组，然鹅模拟退火。

~~不会呀！不会就[现学](https://oi.wiki/misc/simulated-annealing/#%E8%BF%87%E7%A8%8B)呀！~~

我们可以构造出一个差分的“谷”，每次考虑将一个元素（不是最小值）扔到另一边去。当然，如果全是最小值得特判。然后没了。

初始温度我设的 $10^{1200}$ ，不然不够，太可怕了。

然后就 TLE 了。

然后我又发现最后三个点只需要设 $10^{300}$ ，然后就 A 了。

```cpp
#include<bits/stdc++.h>
using namespace std;
int n;
int ans;
const int maxn=1e4+5;
int a[maxn],b[maxn];
int mn;
deque<int>k;
int l,r;
int calc(){
	for(int i=2;i<=n;i++)a[i]=a[i-1]+k[i-2];
	int x=0,y=0;
	for(int i=1;i<=n;i++)x+=a[i],y+=a[i]*a[i];
	return n*y-x*x;
}
mt19937 mt(chrono::system_clock::now().time_since_epoch().count());
void SA(){
	long double T=1e300;
	if(n<=1000)T*=T,T*=T;
	int now=ans=calc();
	int rndsz=n-2-(r-l),len=r-l;
	while(T>1e-5){
		for(int i=0;i<n-1;i++)
			if(k[i]==mn){
				l=i,r=i+len;
				break;
			}
		deque<int>tmp=k;
		int x=uniform_int_distribution<int>(0,rndsz)(mt);
		if(x>=l){
			x+=len;
			int v=k[x];
			k.erase(k.begin()+x);
			auto now=k.begin();
			while(*now>v)++now;
			k.insert(now,v);
		}else{
			int v=k[x];
			k.erase(k.begin()+x);
			auto now=k.end();
			while(*(now-1)>v)--now;
			k.insert(now,v);
		}
		int nwans=calc(),diff=nwans-now;
		if(diff<0||exp(-diff/T)*UINT_MAX>mt())ans=min(ans,now=nwans);
		else k=tmp;
		T*=0.98;
	}
}
int main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);
	cin>>n;for(int i=1;i<=n;i++)cin>>a[i],b[i-1]=a[i]-a[i-1];
	sort(b+1,b+n);
	for(int i=1;i<n;i++)if(i&1)k.push_back(b[i]);else k.push_front(b[i]);
	mn=*min_element(b+1,b+1+n);
	{
		bool flag=0;
		for(auto i=0;i<n-1;i++)
			if(k[i]==mn){
				if(!flag)
					flag=1,l=i;
				r=i+1;
			}
		if(l==0&&r==n-1)return cout<<calc(),0;
	}
	SA();
	cout<<ans;
	return 0;
}
```

## B
- 关于 SPFA，它死了。
### 海拔为 $1$
$p\ge 1$ 是最短路，$p=0$ 是 $0$。

### 链
随便跳。

### 树
倍增。我也不知道怎么倍增。

### 离线
降雨量大到小排序，把能开车的边加上就行。

### 正解
先 dijkstra 求出所有点到 $1$ 的距离。
kruskal 重构树，以海拔为关键字跑最大生成树的 kruskal 重构树，就跑出来一个*小根堆*，对重构树跑 dfs，求出当前子树中距离 $1$ 最短的那个距离，查询时倍增（因为是**小根堆**）可以最大到哪个子树中所有点都能开车到。然后输出前面求出的子树距离就行。

```cpp
#define pbds
#define many
#ifndef pbds
#include<bits/stdc++.h>
using namespace std;
using namespace __gnu_cxx;
#define pqueue priority_queue
template<typename Tp>
using heap=pqueue<Tp,vector<Tp>,greater<Tp>>;
#else
#include<bits/extc++.h>
using namespace std;
using namespace __gnu_cxx;
using namespace __gnu_pbds;
#define unordered_map __gnu_pbds::gp_hash_table
#define pqueue __gnu_pbds::priority_queue
typedef trie<string,null_type,trie_string_access_traits<>,pat_trie_tag,trie_prefix_search_node_update>Trie;
template<typename Tp>
using heap=pqueue<Tp,greater<Tp>>;
#endif
typedef long long ll;
typedef unsigned long long ull;
typedef pair<int,int>pii;
typedef pair<ll,ll>pll;
typedef pair<ull,ull>pull;
template<size_t _Nw>
using arri=array<int,_Nw>;
template<size_t _Nw>
using arrl=array<ll,_Nw>;
mt19937 mt(chrono::system_clock::now().time_since_epoch().count());
mt19937_64 mt64(chrono::system_clock::now().time_since_epoch().count());
const bool mdsw=true;
const int mod=mdsw?998244353:(int)(1e9+7);
template<typename T>
T inf=INFINITY;
template<>
ll inf<ll> =0x3f3f3f3f3f3f3f3f;
template<>
int inf<int> =0x3f3f3f3f;
template<>
short inf<short> =0x3f3f;
template<>
char inf<char> =0x3f;
template<>
ull inf<ull> =0x3f3f3f3f3f3f3f3f;
template<>
unsigned int inf<unsigned int> =0x3f3f3f3f;
template<>
unsigned short inf<unsigned short> =0x3f3f;
template<>
unsigned char inf<unsigned char> =0x3f;
const int maxn=2e5+5;
int n,m,q,k,s,cnt;
vector<array<int,3>>G[maxn];
vector<int>T[maxn<<1];
vector<array<int,4>>E;
int f[maxn<<1],dis[maxn<<1],fa[20][maxn<<1],val[maxn<<1];
int find(int u){return f[u]==u?u:f[u]=find(f[u]);}
void kruskal(){
	sort(E.begin(),E.end(),greater<>());
	cnt=n;
	ll tot=0;
	for(auto[a,l,u,v]:E){
		if(find(u)!=find(v)){
			u=find(u),v=find(v);
			f[u]=f[v]=++cnt,++tot;
			T[cnt].push_back(u);
			T[cnt].push_back(v);
			val[cnt]=a;
		}
		if(tot==n-1)break;
	}
}
void dijkstra(){
	fill(dis+2,dis+2*n,0x3f3f3f3f);
	heap<array<int,2>>pq;
	pq.push({0,1});
	while(pq.size()){
		auto[d,u]=pq.top();
		pq.pop();
		if(d>dis[u])continue;
		for(auto[v,l,a]:G[u])
			if(dis[v]>d+l){
				dis[v]=d+l;
				pq.push({d+l,v});
			}
	}
}
void dfs(int u){
	for(int i=1;i<20;i++)fa[i][u]=fa[i-1][fa[i-1][u]];
	for(auto v:T[u]){fa[0][v]=u;dfs(v);dis[u]=min(dis[u],dis[v]);}
}
int getans(int v,int p){
	for(int i=19;i>=0;i--)if(val[fa[i][v]]>p)v=fa[i][v];
	return dis[v];
}
void solve(){
	cin>>n>>m;
	for(int i=1;i<=n;i++)G[i].clear();E.clear();
	for(int i=1;i<2*n;i++)T[i].clear(),f[i]=i;
	for(int i=1;i<=m;i++){
		int u,v,l,a;
		cin>>u>>v>>l>>a;
		E.push_back({a,l,u,v});
		G[u].push_back({v,l,a});
		G[v].push_back({u,l,a});
	}kruskal();dijkstra();dfs(2*n-1);
	cin>>q>>k>>s;
	int lstans=0;
	while(q--){
		int v,p;
		cin>>v>>p;
		v=(v+k*lstans-1)%n+1,p=(p+k*lstans)%(s+1);
		cout<<(lstans=getans(v,p))<<"\n";
	}
}
int main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);
	int oT_To;
	for(
#ifdef many
	cin>>oT_To
#else
	oT_To=1
#endif
	;oT_To--;solve());
	long double some_Random_in_double=uniform_real_distribution<long double>(mt64(),mt64())(mt64);
	if(some_Random_in_double==NAN||some_Random_in_double==INFINITY||abs(some_Random_in_double-some_Random_in_double)>114514)return 114514;
	else if(1+1==3)exit(0x3f3f3f3f);
	else if(clock()==66666)return clock();
	else if(!time(nullptr))return 1919810;
	else return 0;
}
```
~~也是 AK 了好吧~~

A 的 dp 也就只能咕一会儿了 ![](https://github.githubassets.com/assets/mona-loading-default-c3c7aad1282f.gif)
